#include <stdio.h>
#include <stdlib.h>


main() {

    int nro;
    while(nro < 1 || nro > 5){
        //a
        nro = getchar(); //no me anda, tengo que ingresa al
        //b
        //scanf("%d", &nro);
    }
}
